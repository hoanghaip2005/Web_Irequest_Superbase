<div class="content-header"><div class="content-header">

    <div class="d-flex justify-content-between align-items-center">    <div class="d-flex justify-content-between align-items-center">

        <div>        <div>

            <h1 class="page-title">Chi tiết yêu cầu #{{request.RequestID}}</h1>            <h1 class="page-title">Request Details</h1>

            <p class="page-subtitle">Xem và xử lý thông tin yêu cầu</p>            <p class="page-subtitle">View and manage request information</p>

        </div>        </div>

        <div>        <div>

            <a href="/requests/assigned" class="btn btn-outline-primary">            <a href="/requests" class="btn btn-outline-primary">

                <i class="fas fa-arrow-left me-2"></i>Quay lại danh sách                <i class="fas fa-arrow-left me-2"></i>Back to List

            </a>            </a>

            {{#if canEdit}}            {{#if canEdit}}

            <a href="/requests/{{request.RequestID}}/edit" class="btn btn-primary">            <a href="/requests/{{request.RequestID}}/edit" class="btn btn-primary">

                <i class="fas fa-edit me-2"></i>Chỉnh sửa                <i class="fas fa-edit me-2"></i>Edit

            </a>            </a>

            {{/if}}            {{/if}}

        </div>        </div>

    </div>    </div>

</div></div>



{{#if success}}{{#if success}}

<div class="alert alert-success alert-dismissible fade show" role="alert"><div class="alert alert-success alert-dismissible fade show" role="alert">

    <i class="fas fa-check-circle me-2"></i>{{success}}    <i class="fas fa-check-circle me-2"></i>{{success}}

    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>

</div></div>

{{/if}}{{/if}}



{{#if error}}{{#if error}}

<div class="alert alert-danger alert-dismissible fade show" role="alert"><div class="alert alert-danger alert-dismissible fade show" role="alert">

    <i class="fas fa-exclamation-circle me-2"></i>{{error}}    <i class="fas fa-exclamation-circle me-2"></i>{{error}}

    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>

</div></div>

{{/if}}{{/if}}



<div class="row"><div class="row">

    <div class="col-lg-8">    <div class="col-lg-8">

        <!-- Thông tin yêu cầu -->        <!-- Request Information -->

        <div class="card">        <div class="card">

            <div class="card-header">            <div class="card-header">

                <div class="d-flex justify-content-between align-items-center">                <h5 class="card-title mb-0">

                    <h5 class="card-title mb-0">                    <i class="fas fa-info-circle me-2"></i>Request Information

                        <i class="fas fa-info-circle me-2"></i>Thông tin yêu cầu                </h5>

                    </h5>            </div>

                    <div>            <div class="card-body">

                        <span class="badge status-badge status-{{request.StatusName}}">                <div class="row mb-3">

                            {{#if (eq request.StatusName "Mới")}}                    <div class="col-sm-3">

                            <i class="fas fa-plus-circle me-1"></i>                        <strong>Title:</strong>

                            {{else if (eq request.StatusName "Đang xử lý")}}                    </div>

                            <i class="fas fa-spinner me-1"></i>                    <div class="col-sm-9">

                            {{else if (eq request.StatusName "Hoàn thành")}}                        {{request.Title}}

                            <i class="fas fa-check-circle me-1"></i>                    </div>

                            {{else if (eq request.StatusName "Từ chối")}}                </div>

                            <i class="fas fa-times-circle me-1"></i>                

                            {{else}}                <div class="row mb-3">

                            <i class="fas fa-pause-circle me-1"></i>                    <div class="col-sm-3">

                            {{/if}}                        <strong>Description:</strong>

                            {{request.StatusName}}                    </div>

                        </span>                    <div class="col-sm-9">

                        <span class="badge priority-badge priority-{{request.PriorityName}} ms-2"                         <div class="text-muted">

                              {{#if request.PriorityColor}}style="background-color: {{request.PriorityColor}}"{{/if}}>                            {{#if request.Description}}

                            {{#if (eq request.PriorityName "Khẩn cấp")}}                                {{{substring request.Description 0 500}}}

                            <i class="fas fa-exclamation-triangle me-1"></i>                                {{#if (gt request.Description.length 500)}}...{{/if}}

                            {{else if (eq request.PriorityName "Cao")}}                            {{else}}

                            <i class="fas fa-arrow-up me-1"></i>                                <em>No description provided</em>

                            {{else if (eq request.PriorityName "Trung bình")}}                            {{/if}}

                            <i class="fas fa-minus me-1"></i>                        </div>

                            {{else}}                    </div>

                            <i class="fas fa-arrow-down me-1"></i>                </div>

                            {{/if}}

                            {{request.PriorityName}}                <div class="row mb-3">

                        </span>                    <div class="col-sm-3">

                    </div>                        <strong>Status:</strong>

                </div>                    </div>

            </div>                    <div class="col-sm-9">

            <div class="card-body">                        <span class="badge bg-{{#ifEquals request.StatusName 'Completed'}}success{{else}}{{#ifEquals request.StatusName 'In Progress'}}warning{{else}}secondary{{/ifEquals}}{{/ifEquals}} fs-6">

                <div class="row mb-3">                            {{request.StatusName}}

                    <div class="col-sm-3">                        </span>

                        <strong>Tiêu đề:</strong>                    </div>

                    </div>                </div>

                    <div class="col-sm-9">

                        <h5 class="text-primary">{{request.Title}}</h5>                <div class="row mb-3">

                    </div>                    <div class="col-sm-3">

                </div>                        <strong>Priority:</strong>

                                    </div>

                <div class="row mb-3">                    <div class="col-sm-9">

                    <div class="col-sm-3">                        <span class="badge bg-{{#ifEquals request.PriorityName 'High'}}danger{{else}}{{#ifEquals request.PriorityName 'Medium'}}warning{{else}}success{{/ifEquals}}{{/ifEquals}} fs-6">

                        <strong>Mô tả:</strong>                            {{request.PriorityName}}

                    </div>                        </span>

                    <div class="col-sm-9">                    </div>

                        <div class="request-description">                </div>

                            {{#if request.Description}}

                                <div class="border p-3 bg-light rounded">                {{#if request.WorkflowName}}

                                    {{{nl2br request.Description}}}                <div class="row mb-3">

                                </div>                    <div class="col-sm-3">

                            {{else}}                        <strong>Workflow:</strong>

                                <em class="text-muted">Không có mô tả</em>                    </div>

                            {{/if}}                    <div class="col-sm-9">

                        </div>                        {{request.WorkflowName}}

                    </div>                    </div>

                </div>                </div>

                {{/if}}

                <div class="row mb-3">

                    <div class="col-sm-3">                <div class="row mb-3">

                        <strong>Người tạo:</strong>                    <div class="col-sm-3">

                    </div>                        <strong>Created By:</strong>

                    <div class="col-sm-9">                    </div>

                        <div class="d-flex align-items-center">                    <div class="col-sm-9">

                            <div class="avatar-circle me-3">                        {{request.CreatedByUser}} ({{request.CreatedByEmail}})

                                {{substring request.CreatedByUser 0 1}}                    </div>

                            </div>                </div>

                            <div>

                                <div class="fw-medium">{{request.CreatedByUser}}</div>                {{#if request.AssignedToUser}}

                                <div class="text-muted small">{{request.CreatedByEmail}}</div>                <div class="row mb-3">

                                {{#if request.DepartmentName}}                    <div class="col-sm-3">

                                <div class="text-muted small">{{request.DepartmentName}}</div>                        <strong>Assigned To:</strong>

                                {{/if}}                    </div>

                            </div>                    <div class="col-sm-9">

                        </div>                        {{request.AssignedToUser}} ({{request.AssignedToEmail}})

                    </div>                    </div>

                </div>                </div>

                {{/if}}

                {{#if request.AssignedToUser}}

                <div class="row mb-3">                <div class="row mb-3">

                    <div class="col-sm-3">                    <div class="col-sm-3">

                        <strong>Được giao cho:</strong>                        <strong>Created:</strong>

                    </div>                    </div>

                    <div class="col-sm-9">                    <div class="col-sm-9">

                        <div class="d-flex align-items-center">                        {{formatDate request.CreatedAt}}

                            <div class="avatar-circle me-3 bg-success">                    </div>

                                {{substring request.AssignedToUser 0 1}}                </div>

                            </div>

                            <div>                <div class="row mb-3">

                                <div class="fw-medium">{{request.AssignedToUser}}</div>                    <div class="col-sm-3">

                                <div class="text-muted small">{{request.AssignedToEmail}}</div>                        <strong>Last Updated:</strong>

                            </div>                    </div>

                        </div>                    <div class="col-sm-9">

                    </div>                        {{formatDate request.UpdatedAt}}

                </div>                    </div>

                {{/if}}                </div>



                {{#if request.WorkflowName}}                {{#if request.AttachmentURL}}

                <div class="row mb-3">                <div class="row mb-3">

                    <div class="col-sm-3">                    <div class="col-sm-3">

                        <strong>Workflow:</strong>                        <strong>Attachment:</strong>

                    </div>                    </div>

                    <div class="col-sm-9">                    <div class="col-sm-9">

                        <span class="badge bg-info">{{request.WorkflowName}}</span>                        <a href="{{request.AttachmentURL}}" target="_blank" class="btn btn-outline-primary btn-sm">

                    </div>                            <i class="fas fa-download me-2"></i>{{request.AttachmentFileName}}

                </div>                        </a>

                {{/if}}                    </div>

                </div>

                <div class="row mb-3">                {{/if}}

                    <div class="col-sm-3">            </div>

                        <strong>Ngày tạo:</strong>        </div>

                    </div>

                    <div class="col-sm-9">        <!-- Comments Section -->

                        <span class="text-muted">{{formatDateTime request.CreatedAt}}</span>        {{#if comments}}

                    </div>        <div class="card mt-4">

                </div>            <div class="card-header">

                <h5 class="card-title mb-0">

                <div class="row mb-3">                    <i class="fas fa-comments me-2"></i>Comments ({{comments.length}})

                    <div class="col-sm-3">                </h5>

                        <strong>Cập nhật cuối:</strong>            </div>

                    </div>            <div class="card-body">

                    <div class="col-sm-9">                {{#each comments}}

                        <span class="text-muted">{{formatDateTime request.UpdatedAt}}</span>                <div class="d-flex mb-3">

                    </div>                    <div class="flex-shrink-0">

                </div>                        <div class="avatar-circle">

                            {{substring UserName 0 1}}

                {{#if request.AttachmentURL}}                        </div>

                <div class="row mb-3">                    </div>

                    <div class="col-sm-3">                    <div class="flex-grow-1 ms-3">

                        <strong>Tập tin đính kèm:</strong>                        <div class="d-flex justify-content-between align-items-start">

                    </div>                            <div>

                    <div class="col-sm-9">                                <strong>{{UserName}}</strong>

                        <a href="{{request.AttachmentURL}}" target="_blank" class="btn btn-outline-primary btn-sm">                                <small class="text-muted ms-2">{{formatDate CreatedAt}}</small>

                            <i class="fas fa-download me-2"></i>{{request.AttachmentFileName}}                            </div>

                            {{#if request.AttachmentFileSize}}                        </div>

                            <small class="text-muted ms-2">({{formatFileSize request.AttachmentFileSize}})</small>                        <div class="mt-1">

                            {{/if}}                            {{CommentText}}

                        </a>                        </div>

                    </div>                    </div>

                </div>                </div>

                {{/if}}                {{#unless @last}}<hr>{{/unless}}

            </div>                {{/each}}

        </div>            </div>

        </div>

        <!-- Bình luận -->        {{/if}}

        <div class="card mt-4">

            <div class="card-header">        <!-- Add Comment -->

                <h5 class="card-title mb-0">        {{#if canComment}}

                    <i class="fas fa-comments me-2"></i>Bình luận {{#if comments}}({{comments.length}}){{/if}}        <div class="card mt-4">

                </h5>            <div class="card-header">

            </div>                <h5 class="card-title mb-0">

            <div class="card-body">                    <i class="fas fa-plus me-2"></i>Add Comment

                {{#if comments}}                </h5>

                {{#each comments}}            </div>

                <div class="comment-item mb-3">            <div class="card-body">

                    <div class="d-flex">                <form action="/requests/{{request.RequestID}}/comments" method="POST">

                        <div class="flex-shrink-0">                    <div class="mb-3">

                            <div class="avatar-circle">                        <textarea class="form-control" name="comment" rows="3" 

                                {{substring UserName 0 1}}                                  placeholder="Add your comment..." required></textarea>

                            </div>                    </div>

                        </div>                    <button type="submit" class="btn btn-primary">

                        <div class="flex-grow-1 ms-3">                        <i class="fas fa-paper-plane me-2"></i>Add Comment

                            <div class="comment-header d-flex justify-content-between align-items-start">                    </button>

                                <div>                </form>

                                    <strong>{{UserName}}</strong>            </div>

                                    <small class="text-muted ms-2">{{formatDateTime CreatedAt}}</small>        </div>

                                </div>        {{/if}}

                            </div>    </div>

                            <div class="comment-content mt-2 p-2 bg-light rounded">

                                {{{nl2br Content}}}    <div class="col-lg-4">

                            </div>        <!-- Actions -->

                        </div>        {{#if canManage}}

                    </div>        <div class="card">

                </div>            <div class="card-header">

                {{#unless @last}}<hr class="my-3">{{/unless}}                <h5 class="card-title mb-0">

                {{/each}}                    <i class="fas fa-cogs me-2"></i>Actions

                {{else}}                </h5>

                <p class="text-muted text-center py-3">            </div>

                    <i class="fas fa-comment-slash fa-2x mb-2"></i><br>            <div class="card-body">

                    Chưa có bình luận nào                {{#unless request.IsStatusFinal}}

                </p>                <div class="mb-3">

                {{/if}}                    <label class="form-label">Change Status</label>

                    <form action="/requests/{{request.RequestID}}/status" method="POST" class="d-inline">

                <!-- Thêm bình luận -->                        <select name="statusId" class="form-select mb-2" onchange="this.form.submit()">

                <div class="add-comment mt-4">                            <option value="">Select Status...</option>

                    <form action="/requests/{{request.RequestID}}/comments" method="POST">                            {{#each statuses}}

                        <div class="mb-3">                            <option value="{{StatusID}}" {{#ifEquals StatusID ../request.StatusID}}selected{{/ifEquals}}>

                            <label for="commentContent" class="form-label">Thêm bình luận</label>                                {{StatusName}}

                            <textarea class="form-control" id="commentContent" name="content" rows="3"                             </option>

                                      placeholder="Nhập bình luận của bạn..." required></textarea>                            {{/each}}

                        </div>                        </select>

                        <button type="submit" class="btn btn-primary">                    </form>

                            <i class="fas fa-paper-plane me-2"></i>Gửi bình luận                </div>

                        </button>

                    </form>                <div class="mb-3">

                </div>                    <label class="form-label">Assign To</label>

            </div>                    <form action="/requests/{{request.RequestID}}/assign" method="POST">

        </div>                        <select name="userId" class="form-select mb-2" onchange="this.form.submit()">

    </div>                            <option value="">Select User...</option>

                            {{#each users}}

    <div class="col-lg-4">                            <option value="{{Id}}" {{#ifEquals Id ../request.AssignedToId}}selected{{/ifEquals}}>

        <!-- Thao tác xử lý -->                                {{UserName}}

        {{#if canProcess}}                            </option>

        <div class="card">                            {{/each}}

            <div class="card-header">                        </select>

                <h5 class="card-title mb-0">                    </form>

                    <i class="fas fa-cogs me-2"></i>Thao tác xử lý                </div>

                </h5>                {{/unless}}

            </div>

            <div class="card-body">                {{#if canDelete}}

                {{#unless request.IsStatusFinal}}                <div class="d-grid gap-2">

                <div class="d-grid gap-2 mb-3">                    <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">

                    {{#if (eq request.StatusName "Mới")}}                        <i class="fas fa-trash me-2"></i>Delete Request

                    <button type="button" class="btn btn-success" onclick="startProcessing({{request.RequestID}})">                    </button>

                        <i class="fas fa-play me-2"></i>Bắt đầu xử lý                </div>

                    </button>                {{/if}}

                    {{/if}}            </div>

                            </div>

                    <button type="button" class="btn btn-success" onclick="approveRequest({{request.RequestID}})">        {{/if}}

                        <i class="fas fa-check me-2"></i>Phê duyệt

                    </button>        <!-- Request History -->

                            {{#if history}}

                    <button type="button" class="btn btn-danger" onclick="rejectRequest({{request.RequestID}})">        <div class="card mt-4">

                        <i class="fas fa-times me-2"></i>Từ chối            <div class="card-header">

                    </button>                <h5 class="card-title mb-0">

                </div>                    <i class="fas fa-history me-2"></i>History

                </h5>

                <hr>            </div>

            <div class="card-body">

                <div class="mb-3">                {{#each history}}

                    <label class="form-label">Thay đổi trạng thái</label>                <div class="d-flex mb-3">

                    <select class="form-select" id="statusSelect" onchange="changeStatus({{request.RequestID}})">                    <div class="flex-shrink-0">

                        <option value="">Chọn trạng thái...</option>                        <i class="fas fa-circle text-primary" style="font-size: 8px; margin-top: 8px;"></i>

                        {{#each statuses}}                    </div>

                        <option value="{{StatusID}}" {{#if (eq StatusID ../request.StatusID)}}selected{{/if}}>                    <div class="flex-grow-1 ms-3">

                            {{StatusName}}                        <div class="fw-semibold">{{Action}}</div>

                        </option>                        <small class="text-muted">

                        {{/each}}                            by {{UserName}} on {{formatDate CreatedAt}}

                    </select>                        </small>

                </div>                        {{#if Description}}

                        <div class="mt-1 small">{{Description}}</div>

                {{#if users}}                        {{/if}}

                <div class="mb-3">                    </div>

                    <label class="form-label">Giao lại cho</label>                </div>

                    <select class="form-select" id="assignSelect" onchange="assignRequest({{request.RequestID}})">                {{#unless @last}}<hr class="my-2">{{/unless}}

                        <option value="">Chọn người xử lý...</option>                {{/each}}

                        {{#each users}}            </div>

                        <option value="{{Id}}" {{#if (eq Id ../request.AssignedUserId)}}selected{{/if}}>        </div>

                            {{UserName}}        {{/if}}

                        </option>    </div>

                        {{/each}}</div>

                    </select>

                </div><!-- Delete Confirmation Modal -->

                {{/if}}{{#if canDelete}}

                {{else}}<div class="modal fade" id="deleteModal" tabindex="-1">

                <div class="alert alert-info">    <div class="modal-dialog">

                    <i class="fas fa-info-circle me-2"></i>        <div class="modal-content">

                    Yêu cầu này đã hoàn tất và không thể chỉnh sửa.            <div class="modal-header">

                </div>                <h5 class="modal-title">Confirm Delete</h5>

                {{/unless}}                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>

            </div>            </div>

        </div>            <div class="modal-body">

        {{/if}}                Are you sure you want to delete this request? This action cannot be undone.

            </div>

        <!-- Lịch sử yêu cầu -->            <div class="modal-footer">

        {{#if history}}                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>

        <div class="card mt-4">                <form action="/requests/{{request.RequestID}}/delete" method="POST" class="d-inline">

            <div class="card-header">                    <button type="submit" class="btn btn-danger">

                <h5 class="card-title mb-0">                        <i class="fas fa-trash me-2"></i>Delete

                    <i class="fas fa-history me-2"></i>Lịch sử thay đổi                    </button>

                </h5>                </form>

            </div>            </div>

            <div class="card-body">        </div>

                <div class="timeline">    </div>

                    {{#each history}}</div>

                    <div class="timeline-item">{{/if}}

                        <div class="timeline-marker bg-primary"></div>

                        <div class="timeline-content"><style>

                            <div class="fw-semibold">{{Action}}</div>.avatar-circle {

                            <small class="text-muted">    width: 40px;

                                bởi {{UserName}} - {{formatDateTime ActionTime}}    height: 40px;

                            </small>    background-color: #6c757d;

                            {{#if Note}}    color: white;

                            <div class="mt-1 small text-muted">{{Note}}</div>    border-radius: 50%;

                            {{/if}}    display: flex;

                        </div>    align-items: center;

                    </div>    justify-content: center;

                    {{/each}}    font-weight: bold;

                </div>    text-transform: uppercase;

            </div>}

        </div></style>
        {{/if}}

        <!-- Workflow Steps -->
        {{#if workflowSteps}}
        <div class="card mt-4">
            <div class="card-header">
                <h5 class="card-title mb-0">
                    <i class="fas fa-project-diagram me-2"></i>Quy trình xử lý
                </h5>
            </div>
            <div class="card-body">
                {{#each workflowSteps}}
                <div class="workflow-step mb-3">
                    <div class="d-flex align-items-center">
                        <div class="workflow-step-number">{{StepOrder}}</div>
                        <div class="ms-3">
                            <div class="fw-medium">{{StepName}}</div>
                            {{#if AssignedUserName}}
                            <small class="text-muted">Người xử lý: {{AssignedUserName}}</small>
                            {{/if}}
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
        {{/if}}
    </div>
</div>

<!-- Approve Modal -->
<div class="modal fade" id="approveModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Phê duyệt yêu cầu</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form id="approveForm">
                <div class="modal-body">
                    <input type="hidden" id="approveRequestId">
                    <div class="mb-3">
                        <label for="approveNote" class="form-label">Ghi chú phê duyệt (tùy chọn)</label>
                        <textarea class="form-control" id="approveNote" rows="3" 
                                  placeholder="Nhập ghi chú khi phê duyệt..."></textarea>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hủy</button>
                    <button type="submit" class="btn btn-success">
                        <i class="fas fa-check me-2"></i>Phê duyệt
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Reject Modal -->
<div class="modal fade" id="rejectModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Từ chối yêu cầu</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form id="rejectForm">
                <div class="modal-body">
                    <input type="hidden" id="rejectRequestId">
                    <div class="mb-3">
                        <label for="rejectNote" class="form-label">Lý do từ chối <span class="text-danger">*</span></label>
                        <textarea class="form-control" id="rejectNote" rows="3" required 
                                  placeholder="Nhập lý do từ chối yêu cầu..."></textarea>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hủy</button>
                    <button type="submit" class="btn btn-danger">
                        <i class="fas fa-times me-2"></i>Từ chối
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
// Approve request function
function approveRequest(requestId) {
    document.getElementById('approveRequestId').value = requestId;
    const modal = new bootstrap.Modal(document.getElementById('approveModal'));
    modal.show();
}

// Reject request function
function rejectRequest(requestId) {
    document.getElementById('rejectRequestId').value = requestId;
    const modal = new bootstrap.Modal(document.getElementById('rejectModal'));
    modal.show();
}

// Start processing function
function startProcessing(requestId) {
    if (confirm('Bắt đầu xử lý yêu cầu này?')) {
        fetch(`/requests/${requestId}/start-processing`, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'Accept': 'application/json'
            }
        })
        .then(response => response.json())
        .then(data => {
            if (data.success) {
                showNotification('success', data.message);
                location.reload();
            } else {
                showNotification('error', data.message);
            }
        })
        .catch(error => {
            showNotification('error', 'Có lỗi xảy ra khi bắt đầu xử lý');
        });
    }
}

// Change status function
function changeStatus(requestId) {
    const statusSelect = document.getElementById('statusSelect');
    const statusId = statusSelect.value;
    
    if (!statusId) return;
    
    fetch(`/requests/${requestId}/status`, {
        method: 'PUT',
        headers: {
            'Content-Type': 'application/json',
            'Accept': 'application/json'
        },
        body: JSON.stringify({ statusId })
    })
    .then(response => response.json())
    .then(data => {
        if (data.success) {
            showNotification('success', 'Đã cập nhật trạng thái');
            location.reload();
        } else {
            showNotification('error', data.message);
        }
    })
    .catch(error => {
        showNotification('error', 'Có lỗi xảy ra khi cập nhật trạng thái');
    });
}

// Assign request function
function assignRequest(requestId) {
    const assignSelect = document.getElementById('assignSelect');
    const userId = assignSelect.value;
    
    if (!userId) return;
    
    fetch(`/requests/${requestId}/assign`, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'Accept': 'application/json'
        },
        body: JSON.stringify({ userId })
    })
    .then(response => response.json())
    .then(data => {
        if (data.success) {
            showNotification('success', 'Đã giao lại yêu cầu');
            location.reload();
        } else {
            showNotification('error', data.message);
        }
    })
    .catch(error => {
        showNotification('error', 'Có lỗi xảy ra khi giao lại yêu cầu');
    });
}

// Handle approve form submission
document.getElementById('approveForm').addEventListener('submit', function(e) {
    e.preventDefault();
    const requestId = document.getElementById('approveRequestId').value;
    const note = document.getElementById('approveNote').value;
    
    fetch(`/requests/${requestId}/approve`, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'Accept': 'application/json'
        },
        body: JSON.stringify({ note })
    })
    .then(response => response.json())
    .then(data => {
        if (data.success) {
            showNotification('success', data.message);
            location.reload();
        } else {
            showNotification('error', data.message);
        }
    })
    .catch(error => {
        showNotification('error', 'Có lỗi xảy ra khi phê duyệt yêu cầu');
    });
    
    bootstrap.Modal.getInstance(document.getElementById('approveModal')).hide();
});

// Handle reject form submission
document.getElementById('rejectForm').addEventListener('submit', function(e) {
    e.preventDefault();
    const requestId = document.getElementById('rejectRequestId').value;
    const note = document.getElementById('rejectNote').value;
    
    if (!note.trim()) {
        showNotification('error', 'Vui lòng nhập lý do từ chối');
        return;
    }
    
    fetch(`/requests/${requestId}/reject`, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'Accept': 'application/json'
        },
        body: JSON.stringify({ note })
    })
    .then(response => response.json())
    .then(data => {
        if (data.success) {
            showNotification('success', data.message);
            location.reload();
        } else {
            showNotification('error', data.message);
        }
    })
    .catch(error => {
        showNotification('error', 'Có lỗi xảy ra khi từ chối yêu cầu');
    });
    
    bootstrap.Modal.getInstance(document.getElementById('rejectModal')).hide();
});

// Notification function
function showNotification(type, message) {
    const notification = document.createElement('div');
    notification.className = `alert alert-${type === 'error' ? 'danger' : type} alert-dismissible fade show position-fixed`;
    notification.style.cssText = 'top: 20px; right: 20px; z-index: 9999; min-width: 300px;';
    notification.innerHTML = `
        ${message}
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    `;
    
    document.body.appendChild(notification);
    
    setTimeout(() => {
        if (notification.parentNode) {
            notification.remove();
        }
    }, 5000);
}
</script>

<style>
.avatar-circle {
    width: 40px;
    height: 40px;
    background-color: #6c757d;
    color: white;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    text-transform: uppercase;
    font-size: 14px;
}

.request-description {
    max-height: 300px;
    overflow-y: auto;
}

.comment-item {
    padding: 10px 0;
}

.comment-content {
    font-size: 14px;
    line-height: 1.5;
}

.timeline {
    position: relative;
    padding-left: 30px;
}

.timeline-item {
    position: relative;
    margin-bottom: 20px;
}

.timeline-marker {
    position: absolute;
    left: -35px;
    top: 5px;
    width: 10px;
    height: 10px;
    border-radius: 50%;
}

.timeline-item:before {
    content: '';
    position: absolute;
    left: -31px;
    top: 15px;
    width: 2px;
    height: calc(100% + 10px);
    background-color: #e9ecef;
}

.timeline-item:last-child:before {
    display: none;
}

.workflow-step-number {
    width: 30px;
    height: 30px;
    border-radius: 50%;
    background-color: #007bff;
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    font-size: 12px;
}

.priority-badge, .status-badge {
    font-size: 0.75em;
}

.priority-badge.priority-Khẩn\ cấp {
    background-color: #dc3545 !important;
}

.priority-badge.priority-Cao {
    background-color: #fd7e14 !important;
}

.priority-badge.priority-Trung\ bình {
    background-color: #ffc107 !important;
    color: #000 !important;
}

.priority-badge.priority-Thấp {
    background-color: #28a745 !important;
}

.status-badge.status-Mới {
    background-color: #6f42c1 !important;
}

.status-badge.status-Đang\ xử\ lý {
    background-color: #17a2b8 !important;
}

.status-badge.status-Hoàn\ thành {
    background-color: #28a745 !important;
}

.status-badge.status-Từ\ chối {
    background-color: #dc3545 !important;
}

.status-badge.status-Tạm\ hoãn {
    background-color: #ffc107 !important;
    color: #000 !important;
}

.card {
    border-radius: 0.5rem;
    border: none;
    box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
}

.content-header {
    margin-bottom: 1.5rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid #e9ecef;
}

.page-title {
    color: #495057;
    font-weight: 600;
}
</style>